{
  "scripts": {
    "build": "npm --prefix server install && npm --prefix server run build && rm -rf dist && cp -r server/dist dist",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "playwright": "^1.59.1"
  }
}
